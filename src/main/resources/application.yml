spring:
  application:
    name: zuul-server
  datasource:
    url: jdbc:h2:file:C:/Users/AA/Desktop/GAME/v1.0-begin/data/zuuldb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:WorldOfZuulSecretKeyForJWTToken2026}
  expiration: ${JWT_EXPIRATION:86400000}

gm:
  secret: '${GM_SECRET:gm123}'
